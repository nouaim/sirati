# المساهمة في سيرتي

شكرًا لاهتمامك. المشروع صغير، والمساهمات فيه مرحّب بها: بلاغ عن عيب، أو إصلاح له، أو
تحسين للوثائق، أو تخطيط أفضل للنص من اليمين إلى اليسار.

**اللغة:** اكتب بالعربية أو بالإنجليزية، أيّهما أسهل عليك.

[English](CONTRIBUTING.en.md)


## قبل أن تكتب سطرًا

1. ابحث في القضايا المفتوحة والمغلقة، فلعلّ ما تريد قوله قيل.
2. وإن لم تجده، فافتح قضية واحدة تصف المشكلة أو الاقتراح. واذكر فيها نظامك (التوزيعة،
   أو macOS/Windows)، وما يطبعه `xelatex --version`، والأمر الذي شغّلته، وما ظهر لك.
   ولأي تغيير كبير انتظر ردًّا قبل أن تبدأ، حفاظًا على وقتك.
3. قضية واحدة، تغيير واحد؛ ولا تخلط إصلاحًا مع إعادة تنسيق.


## الخطوات

```bash
# ١. انسخ المستودع إلى حسابك بزرّ Fork، ثم
git clone https://github.com/<اسمك>/sirati && cd sirati
git remote add upstream https://github.com/nouaim/sirati

# ٢. أنشئ فرعًا باسم معبّر
git checkout -b fix/paper-tint-contrast

# ٣. جهّز البيئة (أحدهما)
./install.sh          # على لينكس (وفرع macOS لم يُجرَّب)
make docker           # على أي نظام فيه Docker

# ٤. نفّذ تغييرك، ثم شغّل الاختبارات ويجب أن تنجح كلها
tests/check-arabic-examples.sh

# ٥. حدّث الوثائق (انظر أدناه)، ثم
git commit -m "..."
git push origin fix/paper-tint-contrast
```

ثم افتح Pull Request على الفرع `main`، واكتب في وصفه: ماذا غيّرت ولماذا، وما اختبرته
بالضبط (الأوامر ومخرجاتها)، وأشر إلى القضية بـ `Closes #رقم`.


## قواعد المشروع

* **العربية أولًا:** `README.md` هو المستند العربي و`README.en.md` أخوه الإنجليزي،
  فأي تغيير في أحدهما يحتاج ما يقابله في الآخر. ولا تخلط لغتين في ملف واحد.
* **الوثائق مُختبرة:** كل أمر في الوثائق شُغِّل فعلًا. فإن أضفت أمرًا فشغّله في حاوية
  نظيفة (`docker run --rm -it ubuntu:latest`) قبل أن تكتبه.
* **بلا صور:** القوالب لا تدعمها، فلا تُضف أي ملف صورة.
* **بلا أسماء ناشرين** في أي ملف، ولا في رسائل الـ commit.
* الورقة بيضاء افتراضيًا، وتظلّ صبغة الشامواه معلَّقة (سطران) حتى تُفعّلها بنفسك.
* لا تُضمّن ملفات خطوط، ولا تستخدم `tikz` أو `tcolorbox`؛ والاختبارات تمنعهما.
* الرخصة CC BY-SA 4.0: بإرسال مساهمة فأنت توافق على نشرها تحتها، مع بقاء إشعارات النسب
  إلى المشروع الأصلي.


## ماذا تفحص الاختبارات؟

`tests/check-arabic-examples.sh` يبني المستندين بـ XeLaTeX ويتحقّق من أن كلًّا منهما في
صفحة واحدة، بلا أخطاء ولا محارف ناقصة ولا تحذيرات bidi؛ ومن أن ملفات PDF والصور
المرفوعة مطابقة للمصادر؛ ومن أن الوثائق تحترم سياسات المشروع (الرخصة والنسب)؛ ومن أن
مسارات التثبيت الموثّقة تعمل فعلًا. شغّلها قبل كل Pull Request.


## الترخيص

بالمساهمة تقبل أن تُنشر مساهمتك تحت رخصة
[CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/)، وأن تبقى إشعارات
النسب إلى [مشروع Awesome CV](https://github.com/posquit0/Awesome-CV) كما هي.
